class Solution {
    public int solution(int[] a, int[] b) {
       int length = a.length;
        // 내적을 저장할 변수
        int n = 0;
        
        for(int i=0;i<length;i++){
            n = n + a[i] * b[i];
        }
        return n;
    }
}

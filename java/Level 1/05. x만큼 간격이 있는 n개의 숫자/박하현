class Solution {
    public long[] solution(int x, int n) {
        long[] answer = new long[n];
		boolean limit = (x >=  -10000000 && x <=10000000 && n<=1000 && n>0);
		if(limit) {
		for(int i = 0 ; i < answer.length ; i++){
			answer[i] = (long)  x * (i + 1);
		} 
		} return answer;  
	}    
}

class Solution {
    public long solution(long n) {        
        long answer = 0;
        double m = Math.sqrt(n);
		if (m % 1 == 0) {
		m = Math.pow(m+1,2);
        answer = (long)m;
		} else {
		answer = -1;
        }
        return answer;
    }
}

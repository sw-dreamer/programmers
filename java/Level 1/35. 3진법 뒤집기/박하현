class Solution {
    public int solution(int n) {
        String m = Integer.toString(n,3);
		String l  = "";
		for(int i = m.length()-1  ; i >= 0 ; i--) {
			l += m.charAt(i);
		}
		int answer = Integer.parseInt(l,3);
        return answer;        
  }
}
